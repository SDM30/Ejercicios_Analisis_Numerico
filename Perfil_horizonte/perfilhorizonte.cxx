#include <iostream>
#include <tuple>
#include <vector>

using TNatural = unsigned int;
using TBuilding = std::tuple<TNatural, TNatural, TNatural>;
using TCity = std::vector<std::tuple<TNatural, TNatural, TNatural>>;

int main()
{
    std::cout << "Hello world!" << std::endl;
    return 0;
}

// Perfil de horizonte

// Auxiliar

// Union Perfiles de horizonte
